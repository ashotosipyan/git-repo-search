Project uses latest React.js version and built with "Create react app" CLI tool

Please first of all run `npm install`

After that `npm start` - your browser will open the project on http://localhost:3000
